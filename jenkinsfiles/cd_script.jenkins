pipeline{
    agent{
        label 'main'
    }
    stages{
        stage("Build"){
            steps{
                echo "======= Build stage ======="
                dir ('docker/toolbox') {
                    sh 'docker build -t antonlastukhin/server:latest .'
                }
            }
            post {
                success{
                    withCredentials([string(credentialsId: 'token_id', variable: 'TOKEN_ID'), string(credentialsId: 'bot_id', variable: 'BOT_ID')]) {
                        dir ('notify') {
                            sh 'python3 notify_the_status.py "$TOKEN_ID"'
                        }
                    }
                }
                failure{
                    echo "========A execution failed========"
                }
            }
        }
        stage("Test"){
            steps{
                echo "======= Test stage ======="
            }
        }
        stage("Deploy"){
            steps{
                echo "======= Deploy ======="
            }
        }
    }
}